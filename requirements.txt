# Interfaccia grafica principale
PyQt6>=6.4.0

# Motore web integrato (usato per l'integrazione di noVNC — opzionale)
PyQt6-WebEngine>=6.4.0

# Gestione connessioni SSH e browser SFTP
paramiko>=2.10.0

# Server FTP locale (Strumenti → Server FTP locale)
pyftpdlib>=1.5.7

# Cifratura credenziali (Strumenti → Imposta password globale)
cryptography>=41.0.0
